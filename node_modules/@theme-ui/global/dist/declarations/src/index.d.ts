import { type ThemeUIStyleObject, type Theme as CoreTheme } from '@theme-ui/core';
export interface GlobalProps {
    styles: ThemeUIStyleObject<CoreTheme>;
}
declare const Global: ({ styles }: GlobalProps) => JSX.Element;
export default Global;
